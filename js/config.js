/**
 * Конфигурация Mini App.
 */

const Config = {
    API_BASE: window.location.origin,
    MIN_BET: 1.0,
    MINES_CELLS: 25,
    MINES_COUNT_MIN: 1,
    MINES_COUNT_MAX: 24,
};

window.CasinoConfig = Config;
